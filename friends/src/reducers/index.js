


const initialState = {
    friends: []
};

const reducer = (statee = initialState, action) => {
    switch(action.type) {
        default:
            return statee;
    }
}

export default reducer;